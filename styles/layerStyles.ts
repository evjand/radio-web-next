import { defineLayerStyles } from '@pandacss/dev'

export const layerStyles = defineLayerStyles({
    container: {
        value: {
            padding: {
                base: '0px 20px',
                tablet: '0px 40px',
                bigTablet: '0px 40px',
                desktop: '0px 186px',
            },
        },
    },
})
