import { defineGlobalStyles } from '@pandacss/dev'

export const globalCss = defineGlobalStyles({
    'html, body': {
        fontFamily: '"NRK Sans Variable", sans-serif;',
        color: 'base.contrastLight',
        backgroundColor: 'base.medium',
    },
})
